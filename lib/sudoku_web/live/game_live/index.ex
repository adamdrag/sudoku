defmodule SudokuWeb.GameLive.Index do
  use SudokuWeb, :live_view

  def mount(_params, _session, socket) do
    {:ok, socket}
  end

  def render(assigns) do
    ~H"""
    <div>fhdkshfkjshfkskfksdk fhdsfhksdjh</div>
    """
  end
end
